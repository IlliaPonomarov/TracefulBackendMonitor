
spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:53127/tbm}
      database: tbm
      auto-index-creation: true
      authentication-database: admin
      write-concern: majority

server:
  port: 8444